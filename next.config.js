/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['carrentuzb.herokuapp.com', 'res.cloudinary.com'],
  },
}

module.exports = nextConfig
